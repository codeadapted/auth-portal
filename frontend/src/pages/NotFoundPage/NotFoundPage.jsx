import React from 'react';

/**
 * NotFoundPage Component
 * 
 * Displays a "Page Not Found" message when the user navigates to an invalid route.
 * This serves as the 404 error page for the application.
 */
const NotFoundPage = () => {
    return (
        <div className="404-page">

            {/* Page Title: Displays "Page Not Found" to inform the user of an invalid route */}
            <h1>Page Not Found</h1>
        
        </div>
    );
};

export default NotFoundPage;
